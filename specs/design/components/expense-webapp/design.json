{
  "name": "expense-webapp",
  "type": "web-application",
  "version": "0.1.0",
  "language": "TypeScript",
  "buildpack": "docker",
  "appPath": "expense-webapp",
  "entrypoint": "deployment/web-application",
  "exposure": "internet",
  "stories": [
    1,
    2,
    3,
    4,
    5,
    6,
    7,
    8,
    9
  ],
  "description": "The single web surface for employees, managers, and finance: submitting and tracking claims, reviewing and deciding on direct reports' claims, and exporting approved claims to payroll. Renders a distinct set of screens per signed-in role. Holds no business logic or credentials of its own — all data and decisions come from expense-api.",
  "dependencies": [
    {
      "kind": "component",
      "name": "expense-api",
      "description": "Calls the Expense Claims API for all claim data, approvals, rejections, receipt URLs, and payroll export.",
      "wiring": {
        "endpoint": {
          "component": "employees-submit-expense5721-expense-api",
          "name": "http",
          "visibility": "project",
          "envBindings": {
            "address": "EXPENSE_API_URL"
          }
        }
      }
    },
    {
      "kind": "platform-resource",
      "name": "thunder-auth",
      "description": "Signs the user in via OIDC + PKCE in the browser and attaches the resulting bearer token to API calls.",
      "resourceType": "thunder-app",
      "wiring": {
        "ref": "employees-submit-8ebd4e77",
        "envBindings": {
          "client_id": "THUNDER_AUTH_CLIENT_ID",
          "issuer": "THUNDER_AUTH_ISSUER",
          "jwks_url": "THUNDER_AUTH_JWKS_URL",
          "scopes": "THUNDER_AUTH_SCOPES"
        }
      }
    }
  ],
  "skillsPinned": [
    "wireframes",
    "react-webapp",
    "astryx-design-system",
    "thunder-authentication"
  ]
}
